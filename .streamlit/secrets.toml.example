# Copie ce fichier en secrets.toml (local) ou colle son contenu dans
# Streamlit Community Cloud > App settings > Secrets.
# secrets.toml est dans .gitignore : il ne doit jamais etre commite.

ODDS_API_KEY = "ta-cle-the-odds-api"

# Obligatoire pour tout deploiement public : sans lui, n'importe qui
# consomme tes credits The Odds API.
APP_PASSWORD = "choisis-un-mot-de-passe-long"
